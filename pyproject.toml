[build-system]
requires = ["setuptools>=61.2"]
build-backend = "setuptools.build_meta"

[project]
name = "local_woodoku"
version = "0.1.0"

[tool.setuptools]
packages = ["woodoku"]
include-package-data = false

[tool.black]
line-length = 120
